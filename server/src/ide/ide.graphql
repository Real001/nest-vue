type Query {
  settings(user: String!): Settings
}

type Ide {
  settings(user: String!): Settings
}

type Settings {
  lang: String!
  theme: String!
}

type Mutation {
  updateSettings(settings: SettingsInput!, idUser: String!): Settings
}

input SettingsInput {
  lang: String!
  theme: String!
}

